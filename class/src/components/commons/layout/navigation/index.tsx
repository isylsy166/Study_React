import styled from "@emotion/styled";

const Wrapper = styled.div`
   height: 100px;
   background-color: beige;
`;

export default function LayoutNavigation() {
   return <Wrapper>여기는 메뉴 입니다.</Wrapper>;
}
